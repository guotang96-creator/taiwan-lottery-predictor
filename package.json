{
  "name": "taiwan-lottery-predictor",
  "version": "1.0.0",
  "private": true,
  "description": "Taiwan Lottery Predictor with official data parser and latest official API updater",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "adm-zip": "^0.5.16"
  },
  "scripts": {
    "parse:downloads": "node scripts/parse_downloads.js",
    "convert:json": "node scripts/convert_to_json.js",
    "fetch:latest": "node scripts/fetch_latest_official.js",
    "update:official": "node scripts/update_official.js",
    "build:data": "npm run parse:downloads && npm run fetch:latest && npm run convert:json && npm run update:official"
  }
}
